# 后端API基础地址
# 如果后端和前端在同一域名下，可以使用相对路径 '/api'（推荐使用代理）
# 如果后端在不同域名或端口，使用完整URL，例�? 'http://localhost:8080/api'
VITE_API_BASE_URL=/api

# 开发环境后端地址（如果使用代理）
# 后端服务运行�?Docker 容器中，端口映射�?8080
VITE_API_PROXY_TARGET=http://localhost:8080
